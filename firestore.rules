rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // النظام يعتمد على Firebase Auth عند تسجيل الدخول، لذا يُسمح فقط
    // للمستخدمين الموثّقين بالقراءة والكتابة.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
